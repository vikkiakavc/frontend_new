import { RightclickDirective } from './rightclick.directive';

describe('RightclickDirective', () => {
  it('should create an instance', () => {
    const directive = new RightclickDirective();
    expect(directive).toBeTruthy();
  });
});
